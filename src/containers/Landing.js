import React from "react";

export function Landing() {
  return (
    <div>
      <span>This is the Landing page.</span>
      <p>
        While you were reading this Bitcoin and Pokemon Data was fetched. React
        has also processed the 2nd and 3rd tab, but the browser is not rendering
        them.
      </p>
    </div>
  );
}

export default Landing;
